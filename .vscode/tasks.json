{
  "tasks": [
    {
      "type": "cppbuild",
      "label": "Compilar Nach",
      "command": "/usr/bin/gcc",
      "args": [
			// "-fdiagnostics-color=always",
			"-g3",
			"-Og",
			"${file}",
			// "${workspaceFolder}/sources/libftprintf.a",
			"-o",
			"${fileDirname}/${fileBasenameNoExtension}.out"
		],
      "options": {
        "cwd": "${fileDirname}"
      },
      "problemMatcher": [
        "$gcc"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "detail": "compilador: /usr/bin/gcc"
    }
  ],
  "version": "2.0.0"
}